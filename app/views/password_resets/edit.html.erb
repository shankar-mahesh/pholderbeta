<%= form_for @user, :url => password_reset_path(params[:id]) do |f| %>

<%= f.label "Please enter your new password:" %> 
<%= f.password_field :password %>

<%= f.label "Reenter your new password:" %>
<%= f.password_field :password_confirmation %>

<%= f.submit %>

<% end %>