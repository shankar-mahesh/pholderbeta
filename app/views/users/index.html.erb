	<% provide(:title, "All users") %>

	<% @users.each do |user| %>
	<div>
	<%= link_to user.name, user_path(user) %>
	<%= link_to "Add friend", user_friends_path(:user_id => current_user.id, :friend_id => user.id), :method => :post %>
	</div>
	<% end %>

